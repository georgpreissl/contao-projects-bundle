services:
    _defaults:
        autoconfigure: true
        autowire: true
        public: false
        bind:
            $projectDir: '%kernel.project_dir%'

    GeorgPreissl\Projects\:
        resource: '../src'
        exclude: '../../{ContentElement,FrontendModule,Model}'

